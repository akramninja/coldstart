global:
  # Short interval: throttling, GC and pool saturation come in bursts that long averages erase.
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: order-service
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["order-service:9090"]
