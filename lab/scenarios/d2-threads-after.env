# Default 2, after: CPU limit sized from the peak measured in d2-threads-before, plus headroom.
# 4 is the starting point: replace it with your measured peak, rounded up.
# Load: k6 -e PROFILE=threads
SCENARIO=d2-threads-after
APP_CPUS=4
APP_MEMORY=2g
JAVA_OPTS_APPEND=-XX:MaxRAMPercentage=70
