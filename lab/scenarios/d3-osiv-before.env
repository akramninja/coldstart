# Default 3, before: open-in-view on (Spring Boot default), entities mapped in the web layer.
# Every order read holds one of Hikari's 10 connections during the 20 ms carrier call.
# Load: k6 -e PROFILE=osiv
SCENARIO=d3-osiv-before
APP_CPUS=2
APP_MEMORY=2g
JAVA_OPTS_APPEND=-XX:MaxRAMPercentage=70
