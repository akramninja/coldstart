# Default 1, before: no heap flag. The JVM takes MaxRAMPercentage=25 of a 2 GiB pod: a 512 MiB heap.
# The team caches the whole catalog (200,000 products).
# Load: k6 -e PROFILE=heap
SCENARIO=d1-heap-before
APP_CPUS=2
APP_MEMORY=2g

SPRING_CACHE_CAFFEINE_SPEC=maximumSize=200000,recordStats
